import { IconButton } from '@material-ui/core/';
import LinkIcon from '@material-ui/icons/Link';

const LinkBtn = () => {
  return (
    <IconButton aria-label="icon: add">
      <LinkIcon />
    </IconButton>
  );
};

export default LinkBtn;
